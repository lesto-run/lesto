{
  "name": "@keel/example-admin",
  "version": "0.0.0",
  "private": true,
  "description": "A runnable end-to-end Keel app: the @keel/admin CRUD backbone wired behind real HTTP routes — a paginated + projected product list and an onMutation audit hook that records every create/update/delete to a queryable audit trail.",
  "type": "module",
  "scripts": {
    "start": "bun run run.ts",
    "serve": "bun run serve.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@keel/admin": "workspace:*",
    "@keel/db": "workspace:*",
    "@keel/kernel": "workspace:*",
    "@keel/migrate": "workspace:*",
    "@keel/runtime": "workspace:*",
    "@keel/web": "workspace:*",
    "better-sqlite3": "^11.10.0",
    "zod": "^4.0.0"
  }
}
