{
  "name": "@volo/example-admin",
  "version": "0.0.0",
  "private": true,
  "description": "A runnable end-to-end Volo app: the @volo/admin CRUD backbone wired behind real HTTP routes — a paginated + projected product list and an onMutation audit hook that records every create/update/delete to a queryable audit trail.",
  "type": "module",
  "scripts": {
    "start": "bun run run.ts",
    "serve": "bun run serve.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@volo/admin": "workspace:*",
    "@volo/db": "workspace:*",
    "@volo/kernel": "workspace:*",
    "@volo/migrate": "workspace:*",
    "@volo/runtime": "workspace:*",
    "@volo/web": "workspace:*",
    "better-sqlite3": "^11.10.0",
    "zod": "^4.0.0"
  }
}
