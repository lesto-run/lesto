{
  "name": "@lesto/example-cache",
  "version": "0.0.0",
  "private": true,
  "description": "A runnable Lesto app: the @lesto/cache read-through journey wired behind real HTTP routes — cached GETs, single-flight on concurrent misses, invalidation, TTL expiry, and the SQL store's sweep — on a persistent SQLite store with an injected clock.",
  "type": "module",
  "scripts": {
    "start": "bun run run.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lesto/cache": "workspace:*",
    "@lesto/runtime": "workspace:*",
    "@lesto/web": "workspace:*",
    "better-sqlite3": "^11.10.0"
  }
}
