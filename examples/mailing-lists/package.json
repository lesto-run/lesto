{
  "name": "@keel/example-mailing-lists",
  "version": "0.0.0",
  "private": true,
  "description": "A runnable end-to-end Keel app: the @keel/mailing-lists double-opt-in + broadcast journey wired behind real HTTP routes — subscribe (rate-limited), confirm, broadcast, unsubscribe — with @keel/mail riding @keel/queue for delivery.",
  "type": "module",
  "scripts": {
    "start": "bun run run.ts",
    "serve": "bun run serve.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@keel/db": "workspace:*",
    "@keel/kernel": "workspace:*",
    "@keel/mail": "workspace:*",
    "@keel/mailing-lists": "workspace:*",
    "@keel/queue": "workspace:*",
    "@keel/ratelimit": "workspace:*",
    "@keel/runtime": "workspace:*",
    "@keel/web": "workspace:*",
    "better-sqlite3": "^11.10.0"
  }
}
