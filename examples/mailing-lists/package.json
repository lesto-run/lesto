{
  "name": "@lesto/example-mailing-lists",
  "version": "0.0.0",
  "private": true,
  "description": "A runnable end-to-end Lesto app: the @lesto/mailing-lists double-opt-in + broadcast journey wired behind real HTTP routes — subscribe (rate-limited), confirm, broadcast, unsubscribe — with @lesto/mail riding @lesto/queue for delivery.",
  "type": "module",
  "scripts": {
    "start": "bun run run.ts",
    "serve": "bun run serve.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lesto/db": "workspace:*",
    "@lesto/kernel": "workspace:*",
    "@lesto/mail": "workspace:*",
    "@lesto/mailing-lists": "workspace:*",
    "@lesto/queue": "workspace:*",
    "@lesto/ratelimit": "workspace:*",
    "@lesto/runtime": "workspace:*",
    "@lesto/web": "workspace:*",
    "better-sqlite3": "^11.10.0"
  }
}
