{
  "name": "@volo/example-mailing-lists",
  "version": "0.0.0",
  "private": true,
  "description": "A runnable end-to-end Volo app: the @volo/mailing-lists double-opt-in + broadcast journey wired behind real HTTP routes — subscribe (rate-limited), confirm, broadcast, unsubscribe — with @volo/mail riding @volo/queue for delivery.",
  "type": "module",
  "scripts": {
    "start": "bun run run.ts",
    "serve": "bun run serve.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@volo/db": "workspace:*",
    "@volo/kernel": "workspace:*",
    "@volo/mail": "workspace:*",
    "@volo/mailing-lists": "workspace:*",
    "@volo/queue": "workspace:*",
    "@volo/ratelimit": "workspace:*",
    "@volo/runtime": "workspace:*",
    "@volo/web": "workspace:*",
    "better-sqlite3": "^11.10.0"
  }
}
