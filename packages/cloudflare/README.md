# @lesto/cloudflare

> Lesto on Cloudflare Workers — adapt the pure dispatcher to a Worker fetch handler, serve static assets, and generate wrangler config from a deploy plan.

Part of **[Lesto](https://lesto.run)**, the batteries-included, agent-native fullstack framework.

```bash
bun add @lesto/cloudflare
```

[Docs](https://docs.lesto.run) · [Agent-readable docs](https://docs.lesto.run/llms.txt)
