{
  "name": "@keel/web",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Keel's MVC request-handling core — a Rails-style controller layer that weds @keel/router and @keel/ui.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "dependencies": {
    "@keel/errors": "workspace:*",
    "@keel/router": "workspace:*",
    "@keel/ui": "workspace:*",
    "react": "^19",
    "react-dom": "^19",
    "zod": "^4.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  }
}
