{
  "name": "@volo/mailing-lists",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Ghost-style subscriber lists — double opt-in and broadcasts, composed on @volo/db, @volo/mail, and @volo/queue.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "dependencies": {
    "@volo/db": "workspace:*",
    "@volo/errors": "workspace:*",
    "@volo/mail": "workspace:*",
    "@volo/migrate": "workspace:*",
    "@volo/queue": "workspace:*"
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  }
}
