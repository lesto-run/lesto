{
  "name": "@volo/sites",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Volo sites & targets — declare many sites over one app, and prerender static ones from the app's own request handler.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  },
  "dependencies": {
    "@volo/errors": "workspace:*"
  }
}
