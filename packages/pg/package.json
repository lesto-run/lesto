{
  "name": "@volo/pg",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "description": "Volo's Postgres driver — adapts a node-postgres Pool to the async @volo/db SqlDatabase seam (ADR 0006).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./translate": {
      "types": "./src/translate.ts",
      "import": "./src/translate.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  },
  "dependencies": {
    "@volo/db": "workspace:*"
  }
}
