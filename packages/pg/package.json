{
  "name": "@keel/pg",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "description": "Keel's Postgres driver — adapts a node-postgres Pool to the async @keel/db SqlDatabase seam (ADR 0006).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  },
  "dependencies": {
    "@keel/db": "workspace:*"
  }
}
