{
  "name": "@volo/content-store",
  "version": "0.0.0",
  "license": "MIT",
  "description": "SUPPORTED (v1 content seam) — Volo content on the SQL substrate — persists @volo/content-core entries to the database and hydrates the runtime from it.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  },
  "dependencies": {
    "@volo/content-core": "workspace:*",
    "@volo/db": "workspace:*",
    "@volo/errors": "workspace:*",
    "@volo/migrate": "workspace:*"
  },
  "devDependencies": {
    "better-sqlite3": "^11.10.0"
  }
}
