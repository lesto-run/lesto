{
  "name": "@lesto/pubsub",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Lesto's in-process publish/subscribe hub — synchronous registration, awaited delivery.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  }
}
