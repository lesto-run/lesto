{
  "name": "@volo/integration",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Volo's built-in integration & E2E suite — boots the real stack over a real socket and exercises the seams unit tests mock.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint test",
    "format": "oxfmt test",
    "format:check": "oxfmt --check test"
  },
  "dependencies": {
    "@volo/auth": "workspace:*",
    "@volo/cache": "workspace:*",
    "@volo/cloudflare": "workspace:*",
    "@volo/db": "workspace:*",
    "@volo/identity": "workspace:*",
    "@volo/mail": "workspace:*",
    "@volo/queue": "workspace:*",
    "@volo/ratelimit": "workspace:*",
    "@volo/kernel": "workspace:*",
    "@volo/migrate": "workspace:*",
    "@volo/observability": "workspace:*",
    "@volo/pg": "workspace:*",
    "@volo/router": "workspace:*",
    "@volo/runtime": "workspace:*",
    "@volo/sites": "workspace:*",
    "@volo/web": "workspace:*",
    "@react-email/components": "^1.0.12",
    "@react-email/render": "^2.0.8",
    "better-sqlite3": "^11.10.0",
    "react": "^19.2.7"
  }
}
