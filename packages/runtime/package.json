{
  "name": "@volo/runtime",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "description": "Volo's transport tier — the node:http server that serves a Volo app, plus a queue-worker runner.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  },
  "dependencies": {
    "@volo/errors": "workspace:*",
    "@volo/kernel": "workspace:*",
    "@volo/queue": "workspace:*",
    "@volo/sites": "workspace:*",
    "@volo/web": "workspace:*"
  },
  "devDependencies": {
    "@volo/observability": "workspace:*"
  }
}
