// A nested, dynamic fixture file route — proves the scan descends subdirectories
// and `[slug]` compiles to the `:slug` param pattern, all via the real CLI wiring.
export default function BlogPost() {
  return null;
}
