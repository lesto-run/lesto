{
  "name": "@volo/webhooks",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Volo webhooks — HMAC-signed outbound delivery (retried on @volo/queue) and inbound signature verification.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "dependencies": {
    "@volo/errors": "workspace:*",
    "@volo/queue": "workspace:*"
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "oxfmt src test",
    "format:check": "oxfmt --check src test"
  }
}
