# Keep the build context lean. Both images (the root app image and
# benchmarks/Dockerfile) install dependencies fresh, so never ship local installs.
node_modules
**/node_modules
.git
var
coverage
**/coverage
dist
**/dist
build
**/build
.next
**/.next
.svelte-kit
**/.svelte-kit
*.png
.playwright-mcp
.devcontainer
